pipeline {
         agent any
         stages {
                 stage('Build') {
                 steps {
                     echo 'Hi, GeekFlare. Starting to build the App.'
                 }}
        
                  stage('test') {
                  steps {
                     echo 'TEsting the application.'
                  }}
                  stage('deploy') {
                  steps {
                     echo 'Deploy the application.'
                  }}
                  
         }}
